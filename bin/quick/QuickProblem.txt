1. Write a program in java (class name QuickSort) which will take input any number of integer number series and using  quick sort algorithm (write the algorithm first) it will sort those number and display them. This problem should be covering multiple iteration of the method which will be called to sort the elements. the concept of parting the element into pivot, less than and greater than must be applied. You can use "arraylist" to store the data and to process them.
